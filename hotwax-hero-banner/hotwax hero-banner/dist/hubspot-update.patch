diff --git a/hotwax-hero-banner/hubspot/hotwax-hero.module/fields.json b/hotwax-hero-banner/hubspot/hotwax-hero.module/fields.json
index cd6ef7c..edafd7c 100644
--- a/hotwax-hero-banner/hubspot/hotwax-hero.module/fields.json
+++ b/hotwax-hero-banner/hubspot/hotwax-hero.module/fields.json
@@ -339,6 +339,93 @@
       }
     ]
   },
+  {
+    "name": "design",
+    "label": "Size & style",
+    "help_text": "Live design controls. Every one updates the banner as you drag it.",
+    "type": "group",
+    "expanded": false,
+    "children": [
+      {
+        "name": "headline_size",
+        "label": "Headline size (desktop)",
+        "type": "number",
+        "display": "slider",
+        "min": 28,
+        "max": 72,
+        "step": 1,
+        "suffix": "px",
+        "default": 48
+      },
+      {
+        "name": "subhead_size",
+        "label": "Sub-headline size",
+        "type": "number",
+        "display": "slider",
+        "min": 13,
+        "max": 24,
+        "step": 1,
+        "suffix": "px",
+        "default": 16
+      },
+      {
+        "name": "card_width",
+        "label": "Card size",
+        "help_text": "Width of the main graphic on desktop. The card keeps its shape, so a wider card is a taller banner.",
+        "type": "number",
+        "display": "slider",
+        "min": 560,
+        "max": 1100,
+        "step": 10,
+        "suffix": "px",
+        "default": 780
+      },
+      {
+        "name": "tilt_angle",
+        "label": "Tilt angle",
+        "help_text": "How far the card rotates in 3D. 0 lays it flat.",
+        "type": "number",
+        "display": "slider",
+        "min": 0,
+        "max": 20,
+        "step": 1,
+        "suffix": "°",
+        "default": 11
+      },
+      {
+        "name": "chip_size",
+        "label": "Floating card size",
+        "type": "number",
+        "display": "slider",
+        "min": 180,
+        "max": 380,
+        "step": 10,
+        "suffix": "px",
+        "default": 280
+      },
+      {
+        "name": "panel_tint",
+        "label": "Panel background",
+        "help_text": "The soft wash behind the graphic. The lower edge blends into the accent colour.",
+        "type": "color",
+        "default": {
+          "color": "#FDECEB",
+          "opacity": 100
+        }
+      },
+      {
+        "name": "panel_pad",
+        "label": "Space above the graphic",
+        "type": "number",
+        "display": "slider",
+        "min": 0,
+        "max": 140,
+        "step": 4,
+        "suffix": "px",
+        "default": 52
+      }
+    ]
+  },
   {
     "name": "settings",
     "label": "Animation & layout",
diff --git a/hotwax-hero-banner/hubspot/hotwax-hero.module/module.css b/hotwax-hero-banner/hubspot/hotwax-hero.module/module.css
index cd0e149..45f8aa8 100644
--- a/hotwax-hero-banner/hubspot/hotwax-hero.module/module.css
+++ b/hotwax-hero-banner/hubspot/hotwax-hero.module/module.css
@@ -24,6 +24,47 @@
 .hws-hero *::before,
 .hws-hero *::after { box-sizing: border-box; }
 
+/* ----------------------------------------------------------
+   Theme armour. HubSpot themes ship global rules — floats from
+   the row-fluid grid, heading/paragraph margins, img sizing —
+   that otherwise reach inside the module and break the layout.
+   These neutralise them; the component rules below still win.
+   ---------------------------------------------------------- */
+.hws-hero * { float: none !important; }
+.hws-hero h1,
+.hws-hero p,
+.hws-hero ul,
+.hws-hero li { margin: 0; padding: 0; }
+.hws-hero ul { list-style: none; }
+/* every interactive state, not just the base — themes style
+   button:hover/:focus/:active, which is what painted a white chip
+   behind the stage markers on hover and left it stuck after a click */
+.hws-hero button,
+.hws-hero button:hover,
+.hws-hero button:focus,
+.hws-hero button:active,
+.hws-hero button:focus-visible,
+.hws-hero button:visited {
+  font: inherit;
+  color: inherit;
+  background: none !important;
+  background-color: transparent !important;
+  border: 0 !important;
+  box-shadow: none !important;
+  text-decoration: none !important;
+  border-radius: 0;
+  cursor: pointer;
+  text-align: inherit;
+  transform: none;
+}
+.hws-hero a { text-decoration: none; }
+.hws-hero img { max-width: 100%; border: 0; }
+/* a theme's `img { height: auto }` would collapse the card to nothing */
+.hws-hero .hws-card__img { width: 100% !important; height: 100% !important; }
+/* the panel must be able to clip the tilted card's bottom corners */
+.hws-hero .hws-panel { overflow: hidden !important; }
+
+
 .hws-hero__inner {
   width: 100%;
   max-width: 1440px;
@@ -62,7 +103,7 @@
 
 .hws-hero__desc {
   margin: 0 0 30px;
-  font-size: 16px;
+  font-size: var(--hws-sub, 16px);
   line-height: 1.45;
   color: var(--hws-ink-soft);
   text-wrap: balance;
@@ -188,9 +229,9 @@
 }
 
 /* ----------------------------------------------------- the tabs */
-.hws-tabs-wrap { display: none; position: relative; z-index: 3; margin: 40px auto 20px; }
+.hws-tabs-wrap { display: none; position: relative; z-index: 3; margin: var(--hws-pad, 52px) auto 28px; }
 @media (min-width: 768px)  { .hws-tabs-wrap { display: block; } }
-@media (min-width: 1280px) { .hws-tabs-wrap { margin-top: 60px; margin-bottom: 30px; } }
+@media (min-width: 1280px) { .hws-tabs-wrap { margin-top: calc(var(--hws-pad, 52px) * 1.46); margin-bottom: 38px; } }
 
 .hws-tabs {
   display: inline-flex;
@@ -258,7 +299,17 @@
 
 .hws-tab:hover .hws-tab__icon { border-color: var(--hws-accent); color: var(--hws-accent); }
 .hws-tab:hover .hws-tab__text { color: var(--hws-ink); }
-.hws-tab:focus-visible { outline: 2px solid var(--hws-accent); outline-offset: 3px; border-radius: 8px; }
+/* focus shows on the marker itself — a rectangle round the whole
+   button reads as a stray box */
+.hws-tab:focus-visible { outline: none; }
+.hws-tab:focus-visible .hws-tab__icon {
+  border-color: var(--hws-accent);
+  color: var(--hws-accent);
+  box-shadow: 0 0 0 4px rgba(255, 58, 45, .22);
+}
+.hws-tab:focus-visible .hws-tab__text { color: var(--hws-ink); }
+.hws-dot:focus-visible { outline: none; }
+.hws-dot-track:focus-within { box-shadow: 0 0 0 2px rgba(255, 58, 45, .25); }
 
 /* long labels would burst the row on narrow desktops */
 @media (min-width: 768px) and (max-width: 1023px) {
@@ -321,14 +372,14 @@
   display: grid;
   grid-template-columns: auto 1fr auto;
   width: 100%;
-  max-width: 540px;
+  max-width: 580px;
   margin: 0 auto;
   perspective: 2400px;
 }
-@media (min-width: 1024px) { .hws-stage { max-width: 580px; } }
-@media (min-width: 1280px) { .hws-stage { max-width: 720px; } }
-@media (min-width: 1600px) { .hws-stage { max-width: 820px; } }
-@media (min-width: 1920px) { .hws-stage { max-width: 900px; } }
+@media (min-width: 1024px) { .hws-stage { max-width: 625px; } }
+@media (min-width: 1280px) { .hws-stage { max-width: var(--hws-card-w, 780px); } }
+@media (min-width: 1600px) { .hws-stage { max-width: calc(var(--hws-card-w, 780px) * 1.13); } }
+@media (min-width: 1920px) { .hws-stage { max-width: calc(var(--hws-card-w, 780px) * 1.23); } }
 
 .hws-cards {
   position: relative;
@@ -338,7 +389,7 @@
 }
 /* the rotated card's bottom corners are pushed below the panel's
    clip edge, so they're shaved off rather than showing */
-@media (min-width: 768px) { .hws-cards { margin-top: 2rem; margin-bottom: -58px; } }
+@media (min-width: 768px) { .hws-cards { margin-top: 2.75rem; margin-bottom: -58px; } }
 
 /* the two ghost cards stacked behind the live one */
 .hws-cards::before,
@@ -360,10 +411,12 @@
 
   /* tilt alternates as the carousel advances */
   .hws-hero:not(.hws-hero--flat) .hws-stage[data-tilt="a"] .hws-cards {
-    transform: translateZ(90px) rotateX(11deg) rotateY(11deg) rotate(-3.5deg);
+    transform: translateZ(90px) rotateX(var(--hws-tilt, 11deg))
+               rotateY(var(--hws-tilt, 11deg)) rotate(calc(var(--hws-tilt, 11deg) * -0.32));
   }
   .hws-hero:not(.hws-hero--flat) .hws-stage[data-tilt="b"] .hws-cards {
-    transform: translateZ(90px) rotateX(11deg) rotateY(-11deg) rotate(3.5deg);
+    transform: translateZ(90px) rotateX(var(--hws-tilt, 11deg))
+               rotateY(calc(var(--hws-tilt, 11deg) * -1)) rotate(calc(var(--hws-tilt, 11deg) * 0.32));
   }
   .hws-stage[data-tilt="a"] .hws-cards::before { transform: translate3d(-34px, 0, -38px); }
   .hws-stage[data-tilt="a"] .hws-cards::after  { transform: translate3d(-17px, 0, -19px); }
@@ -456,7 +509,7 @@
     display: block;
     position: relative;
     width: max-content;
-    max-width: 280px;
+    max-width: var(--hws-chip-w, 280px);
     border-radius: 16px;
     background: #fff;
     text-decoration: none;
@@ -469,7 +522,8 @@
 }
 
 /* typed info card: LABEL / value / caption */
-.hws-chip--text { padding: 17px 23px 19px; }
+/* the panel is centre-aligned; chips must not inherit that */
+.hws-chip--text { padding: 17px 23px 19px; text-align: left; }
 .hws-chip__label,
 .hws-chip__value,
 .hws-chip__caption { display: block; }
@@ -577,7 +631,7 @@
 /* ----------------------------------------------------- mobile */
 @media (min-width: 768px) {
   .hws-hero__content { margin-top: 60px; margin-bottom: 60px; }
-  .hws-hero__title { font-size: 48px; margin-bottom: 12px; }
+  .hws-hero__title { font-size: var(--hws-h1, 48px); margin-bottom: 12px; }
   .hws-hero__desc { margin-bottom: 32px; }
 }
 @media (max-width: 767px) {
diff --git a/hotwax-hero-banner/hubspot/hotwax-hero.module/module.html b/hotwax-hero-banner/hubspot/hotwax-hero.module/module.html
index e6e557a..a741424 100644
--- a/hotwax-hero-banner/hubspot/hotwax-hero.module/module.html
+++ b/hotwax-hero-banner/hubspot/hotwax-hero.module/module.html
@@ -1,3 +1,12 @@
+{# Fonts the design needs. Without this the module inherits whatever the
+   theme provides and the type falls back to system sans. #}
+{% require_head %}
+  <link rel="preconnect" href="https://fonts.googleapis.com">
+  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
+  <link rel="stylesheet"
+        href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Sora:wght@600;700&display=swap">
+{% end_require_head %}
+
 {# ============================================================
    HotWax Hero Banner
    Card images, tab labels, copy and logos all come from fields.
@@ -6,11 +15,26 @@
    ============================================================ #}
 
 {% set s          = module.settings %}
+{% set d          = module.design %}
 {% set duration   = (s.slide_duration|float * 1000)|round|int %}
 {% set accent     = s.accent_color.color|default('#FF3A2D') %}
 {% set ink        = s.ink_color.color|default('#212429') %}
 {% set slide_list = module.slides %}
 
+{# Design controls ride in as CSS variables, so every slider updates the
+   banner live without touching the stylesheet. #}
+{% set vars %}
+--hws-accent: {{ accent }};
+--hws-ink: {{ ink }};
+--hws-tint: {{ d.panel_tint.color|default('#FDECEB') }};
+--hws-h1: {{ d.headline_size|default(48) }}px;
+--hws-sub: {{ d.subhead_size|default(16) }}px;
+--hws-card-w: {{ d.card_width|default(780) }}px;
+--hws-chip-w: {{ d.chip_size|default(280) }}px;
+--hws-tilt: {{ d.tilt_angle|default(11) }}deg;
+--hws-pad: {{ d.panel_pad|default(52) }}px;
+{% endset %}
+
 {# One floating info card. Renders typed content or an image, and wraps
    the whole thing in a link when one is set. #}
 {% macro render_chip(chip) %}
@@ -35,7 +59,7 @@
          data-hws-hero
          data-duration="{{ duration }}"
          data-pause-on-hover="{{ 'true' if s.pause_on_hover else 'false' }}"
-         style="--hws-accent: {{ accent }}; --hws-ink: {{ ink }};">
+         style="{{ vars }}">
 
   {# ---------------------------------------------------- copy #}
   <div class="hws-hero__inner">
diff --git a/hotwax-hero-banner/hubspot/hotwax-hero.module/module.js b/hotwax-hero-banner/hubspot/hotwax-hero.module/module.js
index 053a51f..ca0ff32 100644
--- a/hotwax-hero-banner/hubspot/hotwax-hero.module/module.js
+++ b/hotwax-hero-banner/hubspot/hotwax-hero.module/module.js
@@ -187,8 +187,16 @@
     initAll();
   }
 
-  // HubSpot's page editor swaps markup in without a reload
-  window.addEventListener("message", function (e) {
-    if (e && e.data && e.data.type === "inlineEditorReady") initAll();
-  });
+  // HubSpot's page editor swaps markup in without a page reload, and
+  // themes sometimes inject modules late. Watch for new instances
+  // instead of relying on a single load event.
+  if ("MutationObserver" in window) {
+    var mo = new MutationObserver(function () {
+      if (document.querySelector("[data-hws-hero]:not([data-hws-ready])")) initAll();
+    });
+    mo.observe(document.documentElement, { childList: true, subtree: true });
+  }
+
+  // belt and braces for slow/late renders
+  window.addEventListener("load", initAll);
 })();
diff --git a/hotwax-hero-banner/hubspot/preview.html b/hotwax-hero-banner/hubspot/preview.html
index 17fb295..aec70b7 100644
--- a/hotwax-hero-banner/hubspot/preview.html
+++ b/hotwax-hero-banner/hubspot/preview.html
@@ -15,7 +15,7 @@
 <div class="page-column">
 
 <section class="hws-hero hws-hero--bleed" data-hws-hero data-duration="5000" data-pause-on-hover="true"
-         style="--hws-accent:#FF3A2D; --hws-ink:#212429;">
+         style="--hws-accent:#FF3A2D; --hws-ink:#212429; --hws-tint:#FDECEB; --hws-h1:48px; --hws-sub:16px; --hws-card-w:780px; --hws-chip-w:280px; --hws-tilt:11deg; --hws-pad:52px;">
   <div class="hws-hero__inner">
     <div class="hws-hero__content">
       <h1 class="hws-hero__title">
diff --git a/hotwax-hero-banner/styles.css b/hotwax-hero-banner/styles.css
index 5bd629b..42a9eb1 100644
--- a/hotwax-hero-banner/styles.css
+++ b/hotwax-hero-banner/styles.css
@@ -207,8 +207,8 @@ img { max-width: 100%; }
    upcoming = hollow node. Progress fills the connectors.
    --------------------------------------------------------- */
 .hero-navigation { display: none; position: relative; z-index: 3; margin: 20px 0; }
-@media (min-width: 768px)  { .hero-navigation { display: block; margin-top: 40px; } }
-@media (min-width: 1280px) { .hero-navigation { margin-top: 60px; margin-bottom: 34px; } }
+@media (min-width: 768px)  { .hero-navigation { display: block; margin-top: 52px; } }
+@media (min-width: 1280px) { .hero-navigation { margin-top: 76px; margin-bottom: 40px; } }
 
 .nav-tabs-wrapper { position: relative; margin: 0 auto; }
 
@@ -270,7 +270,13 @@ img { max-width: 100%; }
 
 .nav-tab:hover .tab-node { border-color: var(--orange); color: var(--orange); }
 .nav-tab:hover .tab-text { color: var(--ink); }
-.nav-tab:focus-visible { outline: 2px solid var(--orange); outline-offset: 3px; border-radius: 8px; }
+.nav-tab:focus-visible { outline: none; }
+.nav-tab:focus-visible .tab-node {
+  border-color: var(--orange);
+  color: var(--orange);
+  box-shadow: 0 0 0 4px rgba(255, 58, 45, .22);
+}
+.nav-tab:focus-visible .tab-text { color: var(--ink); }
 
 /* long labels would burst the pill on narrow desktops */
 @media (min-width: 768px) and (max-width: 1023px) {
@@ -348,11 +354,11 @@ img { max-width: 100%; }
   margin: 0 auto;
   perspective: 2400px;
 }
-@media (min-width: 768px)  { .cards-showcase { max-width: 540px; } }
-@media (min-width: 1024px) { .cards-showcase { max-width: 580px; } }
-@media (min-width: 1280px) { .cards-showcase { max-width: 720px; } }
-@media (min-width: 1600px) { .cards-showcase { max-width: 820px; } }
-@media (min-width: 1920px) { .cards-showcase { max-width: 900px; } }
+@media (min-width: 768px)  { .cards-showcase { max-width: 580px; } }
+@media (min-width: 1024px) { .cards-showcase { max-width: 625px; } }
+@media (min-width: 1280px) { .cards-showcase { max-width: 780px; } }
+@media (min-width: 1600px) { .cards-showcase { max-width: 880px; } }
+@media (min-width: 1920px) { .cards-showcase { max-width: 960px; } }
 
 .cards-stack {
   position: relative;
@@ -361,7 +367,7 @@ img { max-width: 100%; }
   transform-origin: center;
   transition: all 1s ease-in-out;
 }
-@media (min-width: 768px)  { .cards-stack { margin-top: 2rem; min-height: 330px; margin-bottom: -58px; } }
+@media (min-width: 768px)  { .cards-stack { margin-top: 2.75rem; min-height: 330px; margin-bottom: -58px; } }
 @media (min-width: 1024px) { .cards-stack { min-height: 355px; } }
 @media (min-width: 1280px) { .cards-stack { min-height: 440px; } }
 @media (min-width: 1600px) { .cards-stack { min-height: 500px; } }
